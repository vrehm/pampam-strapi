{
    "host": "${process.env.HOST}",
    "port": "${process.env.PORT}",
    "production": true,
    "proxy": {
        "enabled": false
    },
    "cron": {
        "enabled": false
    },
    "admin": {
        "autoOpen": false
    }
}